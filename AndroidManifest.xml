<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.gr.java_conf.Timedrop.achievecollection"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GpsActivity"
            android:description="@string/gps"
            android:label="@string/gps" >
        </activity>
        <activity
            android:name=".VeneziaActivity"
            android:description="@string/venezia"
            android:label="@string/venezia" >
        </activity>
        <activity
            android:name=".FirenzeActivity"
            android:description="@string/firenze"
            android:label="@string/firenze" >
        </activity>
        <activity
            android:name=".RomeActivity"
            android:description="@string/rome"
            android:label="@string/rome" >
        </activity>
        <activity
            android:name=".SqlActivity"
            android:description="@string/sql"
            android:label="@string/sql" >
        </activity>
    </application>

</manifest>
